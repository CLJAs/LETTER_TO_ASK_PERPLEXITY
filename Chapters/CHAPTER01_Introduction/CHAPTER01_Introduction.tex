\chapter{Introduction}

	\noindent
	The general proccess, begins KNOWING perfectly the set we are talking about. We need to understand HOW their elements are constructed, and their combinatorics.
	\\\\
	
	\noindent
	In Theory of Languages, formal languages are two sets: one set contains the alphabet of symbols we are going to use; and the other set contains all chains we decided the language contains, using JUST the symbols of the previous alphabet to create the chains. We are going to turn tables. NOW, sets are languages. We are going to define a particular alphabet ( finite or infinite ) of symbols for our set, and the elements of the set, are going to be CHAINS of those symbols... finite chains, or INFINITE chains. We are going to call our symbols, LAMBDAS, and each element is going to be in the format of a t-upla of lambdas:\\\\
	\textless ** \textit{See unnecesary comment 0001, in case you want to know WHY we call them lambdas} \textgreater\\
	$$ (\lambda_{1}, \lambda_{2}, \lambda_{3}, \lambda_{4}, ... , \lambda_{m}) $$\\
	$ \lambda_{1} $ and $\lambda_{4}$ can be the same symbol of our alphabet. Lambdas are more like variables that can contain any possible symbol of our alphabet.
	\\\\
	
	\noindent
	The second step is designing a Construction LJA for our set. And this is hard. We need to design the alphabet, and the properties of the chains, to fit our set inside a particular CLJA. Or another set, that contains our studied set. We want just to prove it HAS NOT a quantity of elements bigger than $\mathbb{N}$ has. So a superset of it works well for us too.
	\\\\
	
	\noindent
	Once we had designed our Language, the set of representations, for our set, that could be inside a CLJA, it will define ANOTHER LANGUAGE, called LCF (wait for it, it is explained after, in this document). LCF only has chains of finite size, and thanks we have it produced by a CLJA, the bijection with N is automatic. Direct and inverse functions. Well defined mathemathically (more or less).
	\\\\
	
	\noindent
	The system of relations is created between our set of representations (the first language), and the language LCF defined by the CLJA (our second language).
	\\\\
	
	\noindent
	We need to provide any possible tool, to prove our studied set, HAS NOT A CARDINAL bigger than our first language (its set of chains). And a second tool to prove our LCF language has not a cardinal bigger than $\mathbb{N}$. It is provided by the bijection the CLJA creates with N, called: FLJA... Function LJA.
	\\\\
	
	\noindent
	CLJAs have an important rule: the alphabet can be infinite, but we need to provide a bijection with $\mathbb{N}$. We can deal with sets with any possible $\aleph$ as its cardinal, but the alphabet we use must have cardinal $\aleph_{0}$, and we need to define the bijection between $\mathbb{N}$ and the alphabet we use, for the language we use, to represent our studied set. Inside FLJAs there is a subfunction called ``Translation function'', and it uses THAT bijection, inside it.
	\\\\
	
	\noindent
	We are not talking anymore about CLJAs. And it hurts me a lot. Why? It could take several days, or hundreds of pages. I will prove ONLY that the bijection between our particular case of LCF and N, MUST EXISTS, based in a very well known result. And we must accept the deal, you will NOT try to change anything. You will accept our representation language, and LCF as they are. Even if they are a little weird. The explanation is based on the CLJA, called CLJA-FTC, that we designed for $\mathcal{P}(\mathbb{N})$ vs $\mathbb{N}$. And we don't need the CLJA to explain the future properties of both languages and what they can do.
	\\\\
	
	\noindent
	\textless ** \textit{See unnecesary comment 0002, to read curious facts about CLJAs, under your own risk :D} \textgreater
	\\\\
	
	\noindent
	The general strategy is:\\
	$$ Card(\{ \text{ORIGINAL SET} \}) \ngtr Card(\{ \text{REPRESENTATION LANGUAGE} \}) \ngtr Card(LCF) \ngtr Card(\mathbb{N}) $$
	And show it by any possible way.
	\\\\
	
	\noindent
	In our particular case:\\
	$$ Card( \mathcal{P}(\mathbb{N}) ) \ngtr Card( \{ \emptyset::\text{as a symbol} \} \cup SNEFs \cup SNEIs) \ngtr Card( \text{PREVIOUS} \cup LCF) \ngtr Card(\mathbb{N}) $$
	\\\\
	
	\noindent
	The next step is called DIVIDE AND CONQUER. We are going to create a partition of each one of the four sets, and after that, we will create paths, between unique subsets of the partition of the original set, and subsets of the partition of $\mathbb{N}$. We won't repeat the use of two subsets, in two different paths, always trying to prove, by any possible way, that the previous subset, HAS NOT a cardinal bigger than the next subset in the path. We don't need to use ALL subsets inside intermediate sets, we just need complete paths, without repeating the use of the subsets.
	\\\\
	
	\noindent
	You are going to hate three words: PAIR, PARTITION and SUBSET.
	\\\\
	
	\noindent
	We will reduce quickly the problem, to one step, in one path, between two subsets: $SNEIs$ and $LCF_{2p}$, partially based in the concept of Naive CA Th. We failed creating it... and while we were failing, the system of relations appeared.
	\\\\
	
	\noindent
	We will use the system of relations to prove $Card(SNEIs) \ngtr Card(LCF_{2p}) $.
	\\\\
	
	\section{ Quick resume of the path of concepts }
		
		\noindent
		\textbf{Step 001}: D-pair, R-pair, complete relations and PACKs.
		\\\\
		
		\noindent
		\textbf{Step 002}: Naive CA th. An alternative tool to injective functions, to prove $ Card(Domain) \ngtr Card(Image) $ 
		\\\\
		
		\noindent
		\textbf{Step 003}: General SCHEME. Image.
		\\\\
		
		\noindent
		\textbf{Step 004}: Explaining the $\mathcal{P}(\mathbb{N})$ partition.
		\\\\
	
		\noindent
		\textbf{Step 005}: Explaining REPRESENTATIONS SET partition: SNEFs and SNEIs. We will introduce here function SI: Initial Sequences. The acronyms are in spanish.
		\\\\
		
		\noindent
		\textbf{Step 006}: Explaining the partition of $\text{PREVIOUS} \cup LCF$. First partition: PREVIOUS and LCF. Second partition: $LCF_{1}$ and $LCF_{2}$. Third partition: $LCF_{2c}$ and $LCF_{2p}$. Fourth partition: Universes $\theta_{k}$, from $LCF_{2p}$.
		\\\\
		
		\noindent
		\textbf{Step 007}: Explaining why MUST exists a bijection between $LCF$ and $\mathbb{N}$.\\
		Bijection $\Omega: (LCF \: \cup \: PREVIOUS) \leftrightarrow \mathbb{N}$.\\
		We won't use its real name: flja\_practical, direct version. This creates automatically a partition in $\mathbb{N}$, that does not need any explanation.
		\\\\
		
		\noindent
		\textbf{Step 008}: Explaining ALL the paths between subsets of $\mathcal{P}(\mathbb{N})$ and subsets of $\mathbb{N}$. We will reduce the problem to prove $ Card(SNEIs) \ngtr Card(LCF_{2p})$. We will use bijections, injectivities, and the Naive CA th, or we will try it. We will fail in the most important step. We will try to create another tool, based in Naive CA Th.
		\\\\
		
		\noindent
		\textbf{Step 009}: Level of conflict $\gamma$ between a pair of SNEIs: $\gamma(SNEI_{a}, SNEI_{b})$.
		\\\\
		
		\noindent
		\textbf{Step 010}: Using $\gamma$ property, to define a partition of $  SNEIs \: X \: SNEIs $. Families $F_{k}$ of pairs of SNEIs.
		\\\\
		
		\noindent
		\textbf{Step 011}: Relation flja\_abstract, between SNEIs and $LCF_{2p}$. General definition, and some concrete examples.
		\\\\
		
		\noindent
		\textbf{Step 012}: How to create the system of relations $r_{\theta_{k}}$ partitioning the relation flja\_abstract.
		\\\\
		
		\noindent
		\textbf{Step 013}: WHY the relation $r_{\theta_{k+1}}$ is able to assign DISJOINT INFINITE PACKs, to all D-pairs of SNEIs inside Families: $ F_{k}, F_{k-1}, F_{k-2}, ... , F_{1}, F_{0}, F_{\infty} $.
		\\\\
		
		\noindent
		\textbf{Step 014}: General table for all relations $r_{\theta_{k}}$ and Families $F_{k}$.
		\\\\
		
		\noindent
		\textbf{Step 015}: WHY this means $ Card(SNEIs) \ngtr Card(LCF_{2p})$??. YOU GAVE ME THE MEANING... in our talk, I got the vibe you understood the meaning of the system of relations. But let me put some final conclussions here. Curious facts about the system of relations, and refreshing quickly your memory.
		\\\\
		
		\noindent
		\textbf{Step 016}: You will revise it. You will see all I said was real and correct. You will call some colleagues and institutions. I reserved a piece of prestige and prizes for the first mathematician BRAVE, HUMBLE and GOOD enough to realize the value of this work, that helped me to break the incredulity wall, and introduce it to mathematical international community. AND... to get resources to study the rest of the work, write it properly in academic style, and obtain a team and resources to DO ALL THE WORK IS NEEDED TO DO. And explore all the new doors it has opened. Solving the Continuum Hypothesis in a definitive way, is just a little detail from all its consequences. And all my branches. That is WHY, talking with you, I ignored the C.H.. After this document, you will be in the same point as me. I hope. If that is the case, I don't want to work in secret, I want it quickly, making a conference WE can do in your prefered SERIOUS institution: you, my partner and me. We need to talk about details and goals, I need to know WHO you are. GLORY awaits us :D.
		\\\\
	
		\noindent
		Let's begin it !!
	
	
	
	
		
	
	
	


\newpage