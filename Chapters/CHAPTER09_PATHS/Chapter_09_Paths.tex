\chapter{Paths}

	\subsection{Path from empty to Zero}
	
		\noindent
		We begin in the columns of subsets of $\mathcal{P(\mathbb{N})}$.
		\\\\
		
		\noindent
		From ONE concept, the philosofical, or mathematical concept of the empty set, to ONE symbol, in the column of Representations, the empty set symbol, $\emptyset$, the bijection is automatic.
		\\\\
		
		\noindent
		In the same row, from Representations to (PREVIOUS U LCF). We have one set with one element, and Previous: \\
		$f: \{ \emptyset \} \longrightarrow \{empty, YvB\}$ \\
		$\emptyset \rightarrow empty$\\
		f is an injectivity.
		\\\\
		
		\noindent
		The word "empty" is related to the natural number 0, by the bijection $\Omega$. In the chapter about $\Omega$ bijection you have links for the firts 300 millions natural numbers: check zero. "YvB" is related to 1.
		\\\\
		
		\noindent
		First path proven and ended.
		\\\\
		
	\subsection{ From subsets of $\mathbb{N}$ with finite cardinal to $LCF_{1}$ }
	
		\noindent
		From the set of all subsets of $\mathbb{N}$ with FINITE cardinal, to SNEFs, the bijection was explained before: there is only one unique possible way to write a subset of $\mathbb{N}$ in growing order.
		\\\\
		
		\noindent
		From SNEFs to $LCF_{1}$, the algorithm to design the bijection is trivial:\\
		$(\lambda_{1}, \lambda_{2}, \lambda_{3}, ..., \lambda_{m})$\\
		$ S=(\lambda_{1}, \lambda_{2}, \lambda_{3}, ..., \lambda_{m})  \Longleftarrow (S,(0))  $\\
		$(  (\lambda_{1}, \lambda_{2}, \lambda_{3}, ..., \lambda_{m}), (0) )$\\
		DR=0 is a constant in members of $LCF_{1}$.
		\\\\
		
		\noindent
		In compact format:\\
		$(\lambda_{1}, \lambda_{2}, \lambda_{3}, ..., \lambda_{m})$\\
		$ S = \lambda_{1}, \lambda_{2}, \lambda_{3}, ..., \lambda_{m}   \Longleftarrow ( \{ S  \}DR0) $\\
		$ (\{\lambda_{1}, \lambda_{2}, \lambda_{3}, ..., \lambda_{m} \}DR0 )$\\
		\\\\
		
		\noindent
		From the zip files with examples of the bijection $\Omega$ I opened one file and choose some examples of members of $LCF_{1}$ and its natural related(lines, copy paste directly, from the txt file, adapted to latex):\\\\
		$215000013  \rightarrow (\{9765,9779,9782\}DR0)$ OK!! \\
		$215000041  \rightarrow (\{9779,9803\}DR0)$ OK!!\\
		$215000239  \rightarrow (\{9878,9894,9895,9896\}DR0)$ OK!!\\
		\\\\
		
		\noindent
		Second path finished and proven.
		\\\\
		
	\subsection{ From subsets of $\mathbb{N}$ with INFINITE cardinal to SNEIs }
	
		\noindent
		Explained before in this chapter: there is only one unique way of writting a subset of $\mathbb{N}$ in growing order. This applies to infinite subsets too. The bijection is automatic.
		\\\\
		
	\subsection{ From $LCF_{2p}$ to some subset of $\mathbb{N}$ }
	
		\noindent
		Controlled by bijection $\Omega$ between (LCF U Previous) and $\mathbb{N}$.
		\\\\
		
	\subsection{ SNEIs vs $LCF_{2p}$ }
	
		\noindent
		Is the unique step that we need to solve to end the third and final path, that will let us DESTROY the Cantor's Theorem. Whe have reduced the problem, but at this point we know the two sets involved, perfectly, and we are aware that other subsets of $\mathbb{N}$ are doing the job for the rest of pieces from $\mathcal{P(\mathbb{N})}$. This is not an equivalent case, we are solving ALL $\mathcal{P(\mathbb{N})}$ only using $\mathbb{N}$. BUT FROM NOW we are focusing only in this two sets.
		\\\\
		
		
	
\newpage
